import { a as __toESM } from "./chunk-_TIqcEvS.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region node_modules/react-infinite-scroll-component/dist/index.es.js
var import_jsx_runtime = require_jsx_runtime();
var import_react = /* @__PURE__ */ __toESM(require_react());
var ThresholdUnits = {
	Pixel: "Pixel",
	Percent: "Percent"
};
var defaultThreshold = {
	unit: ThresholdUnits.Percent,
	value: 80
};
function parseThreshold(scrollThreshold) {
	if (typeof scrollThreshold === "number") return {
		unit: ThresholdUnits.Percent,
		value: scrollThreshold * 100
	};
	if (typeof scrollThreshold === "string") {
		if (/^(\d*(\.\d+)?)px$/.exec(scrollThreshold)) return {
			unit: ThresholdUnits.Pixel,
			value: parseFloat(scrollThreshold)
		};
		if (/^(\d*(\.\d+)?)%$/.exec(scrollThreshold)) return {
			unit: ThresholdUnits.Percent,
			value: parseFloat(scrollThreshold)
		};
		console.warn("scrollThreshold format is invalid. Valid formats: \"120px\", \"50%\"...");
		return defaultThreshold;
	}
	console.warn("scrollThreshold should be string or number");
	return defaultThreshold;
}
/**
* Converts a scrollThreshold value into a CSS rootMargin string for IntersectionObserver.
*
* scrollThreshold represents "how far down the page before triggering" (e.g. 0.8 = 80%).
* IntersectionObserver's rootMargin extends the root's bounding box so the sentinel is
* detected before it actually enters the visible area.
*
* For a percentage threshold (e.g. 0.8 → 80%):
*   remaining = 100% - 80% = 20% → rootMargin bottom = "20%"
*
* For a pixel threshold (e.g. "120px"):
*   rootMargin bottom = "120px"
*
* Note: rootMargin percentages are relative to the root element's own dimensions, not
* scrollHeight. This is a close approximation of the original scroll-event behavior and
* is an intentional tradeoff for the performance benefit.
*
* For inverse mode the margin is applied to the top instead of the bottom.
*/
function buildRootMargin(scrollThreshold, inverse) {
	const threshold = parseThreshold(scrollThreshold);
	let margin;
	if (threshold.unit === ThresholdUnits.Pixel) margin = `${threshold.value}px`;
	else margin = `${100 - threshold.value}%`;
	return inverse ? `${margin} 0px 0px 0px` : `0px 0px ${margin} 0px`;
}
/**
* Low-level hook for building custom infinite scroll UIs.
*
* Manages an IntersectionObserver that watches a sentinel element you place
* at the end of your list. When the sentinel enters the viewport, next() is
* called. The hook handles deduplication and resets automatically when
* dataLength changes.
*
* Use the InfiniteScroll component instead if you want a ready-made wrapper
* with built-in loader, endMessage, pull-to-refresh, and inverse scroll UI.
*
* @example Basic usage
* ```tsx
* function Feed() {
*   const [items, setItems] = useState(initialItems);
*   const [hasMore, setHasMore] = useState(true);
*
*   const { sentinelRef, isLoading } = useInfiniteScroll({
*     next: async () => {
*       const more = await fetchItems(items.length);
*       if (more.length === 0) { setHasMore(false); return; }
*       setItems(prev => [...prev, ...more]);
*     },
*     hasMore,
*     dataLength: items.length,
*   });
*
*   return (
*     <ul>
*       {items.map(item => <li key={item.id}>{item.name}</li>)}
*       <li ref={sentinelRef} aria-hidden />
*       {isLoading && <li>Loading...</li>}
*     </ul>
*   );
* }
* ```
*/
function useInfiniteScroll({ next, hasMore, dataLength, scrollThreshold = .8, scrollableTarget, inverse = false }) {
	const [isLoading, setIsLoading] = (0, import_react.useState)(false);
	const sentinelRef = (0, import_react.useRef)(null);
	const actionTriggeredRef = (0, import_react.useRef)(false);
	const nextRef = (0, import_react.useRef)(next);
	nextRef.current = next;
	const getScrollableNode = (0, import_react.useCallback)(() => {
		if (scrollableTarget instanceof HTMLElement) return scrollableTarget;
		if (typeof scrollableTarget === "string") return document.getElementById(scrollableTarget);
		return null;
	}, [scrollableTarget]);
	(0, import_react.useEffect)(() => {
		actionTriggeredRef.current = false;
		setIsLoading(false);
	}, [dataLength]);
	(0, import_react.useEffect)(() => {
		if (!hasMore) return;
		if (typeof IntersectionObserver === "undefined") return;
		const sentinel = sentinelRef.current;
		if (!sentinel) return;
		const root = getScrollableNode();
		const observer = new IntersectionObserver(([entry]) => {
			if (!entry.isIntersecting || actionTriggeredRef.current) return;
			actionTriggeredRef.current = true;
			setIsLoading(true);
			nextRef.current();
		}, {
			root,
			rootMargin: buildRootMargin(scrollThreshold, inverse),
			threshold: 0
		});
		observer.observe(sentinel);
		return () => observer.disconnect();
	}, [
		hasMore,
		scrollThreshold,
		inverse,
		getScrollableNode
	]);
	return {
		sentinelRef,
		isLoading
	};
}
function InfiniteScroll({ next, hasMore, children, loader, scrollThreshold = .8, endMessage, style, height, scrollableTarget, hasChildren, inverse = false, pullDownToRefresh = false, pullDownToRefreshContent, releaseToRefreshContent, pullDownToRefreshThreshold = 100, refreshFunction, onScroll, dataLength, initialScrollY, className = "", role, tabIndex, id, ...ariaProps }) {
	const [showLoader, setShowLoader] = (0, import_react.useState)(false);
	const [pullToRefreshThresholdBreached, setPullToRefreshThresholdBreached] = (0, import_react.useState)(false);
	const [maxPullDownDistance, setMaxPullDownDistance] = (0, import_react.useState)(0);
	const infScrollRef = (0, import_react.useRef)(null);
	const sentinelRef = (0, import_react.useRef)(null);
	const pullDownRef = (0, import_react.useRef)(null);
	const nextRef = (0, import_react.useRef)(next);
	nextRef.current = next;
	const refreshFunctionRef = (0, import_react.useRef)(refreshFunction);
	refreshFunctionRef.current = refreshFunction;
	const pullThresholdRef = (0, import_react.useRef)(pullDownToRefreshThreshold);
	pullThresholdRef.current = pullDownToRefreshThreshold;
	const actionTriggeredRef = (0, import_react.useRef)(false);
	const draggingRef = (0, import_react.useRef)(false);
	const startYRef = (0, import_react.useRef)(0);
	const currentYRef = (0, import_react.useRef)(0);
	const maxPullDownDistanceRef = (0, import_react.useRef)(0);
	const getScrollableNode = (0, import_react.useCallback)(() => {
		if (scrollableTarget instanceof HTMLElement) return scrollableTarget;
		if (typeof scrollableTarget === "string") return document.getElementById(scrollableTarget);
		if (scrollableTarget === null) console.warn(`You are trying to pass scrollableTarget but it is null. This might
        happen because the element may not have been added to DOM yet.
        See https://github.com/ankeetmaini/react-infinite-scroll-component/issues/59 for more info.
      `);
		return null;
	}, [scrollableTarget]);
	(0, import_react.useEffect)(() => {
		if (typeof dataLength === "undefined") throw new Error("mandatory prop \"dataLength\" is missing. The prop is needed when loading more content. Check README.md for usage");
		if (pullDownToRefresh && typeof refreshFunction !== "function") throw new Error(`Mandatory prop "refreshFunction" missing.
          Pull Down To Refresh functionality will not work
          as expected. Check README.md for usage'`);
		if (typeof initialScrollY === "number") {
			const el = height ? infScrollRef.current : getScrollableNode();
			if (el && el.scrollHeight > initialScrollY) el.scrollTo(0, initialScrollY);
		}
	}, []);
	(0, import_react.useEffect)(() => {
		actionTriggeredRef.current = false;
		setShowLoader(false);
	}, [dataLength]);
	(0, import_react.useEffect)(() => {
		if (!hasMore) return;
		if (typeof IntersectionObserver === "undefined") return;
		const sentinel = sentinelRef.current;
		if (!sentinel) return;
		const root = height ? infScrollRef.current : getScrollableNode();
		const observer = new IntersectionObserver(([entry]) => {
			if (!entry.isIntersecting || actionTriggeredRef.current) return;
			actionTriggeredRef.current = true;
			setShowLoader(true);
			nextRef.current();
		}, {
			root,
			rootMargin: buildRootMargin(scrollThreshold, inverse),
			threshold: 0
		});
		observer.observe(sentinel);
		return () => observer.disconnect();
	}, [
		hasMore,
		scrollThreshold,
		inverse,
		height,
		getScrollableNode
	]);
	(0, import_react.useEffect)(() => {
		var _a;
		if (!onScroll) return;
		const scrollEl = (_a = height ? infScrollRef.current : getScrollableNode()) !== null && _a !== void 0 ? _a : typeof window !== "undefined" ? window : null;
		if (!scrollEl) return;
		const handler = (e) => {
			setTimeout(() => onScroll(e), 0);
		};
		scrollEl.addEventListener("scroll", handler);
		return () => scrollEl.removeEventListener("scroll", handler);
	}, [
		onScroll,
		height,
		getScrollableNode
	]);
	(0, import_react.useEffect)(() => {
		var _a, _b;
		if (!pullDownToRefresh) return;
		const scrollEl = (_a = height ? infScrollRef.current : getScrollableNode()) !== null && _a !== void 0 ? _a : typeof window !== "undefined" ? window : null;
		if (!scrollEl) return;
		if ((_b = pullDownRef.current) === null || _b === void 0 ? void 0 : _b.firstChild) {
			const dist = pullDownRef.current.firstChild.getBoundingClientRect().height;
			maxPullDownDistanceRef.current = dist;
			setMaxPullDownDistance(dist);
		}
		const onStart = (evt) => {
			if ((scrollEl instanceof HTMLElement ? scrollEl.scrollTop : document.documentElement.scrollTop) > 0) return;
			draggingRef.current = true;
			if (evt instanceof MouseEvent) startYRef.current = evt.pageY;
			else if (evt instanceof TouchEvent) startYRef.current = evt.touches[0].pageY;
			currentYRef.current = startYRef.current;
			if (infScrollRef.current) {
				infScrollRef.current.style.willChange = "transform";
				infScrollRef.current.style.transition = "transform 0.2s cubic-bezier(0,0,0.31,1)";
			}
		};
		const onMove = (evt) => {
			if (!draggingRef.current) return;
			if (evt instanceof MouseEvent) currentYRef.current = evt.pageY;
			else if (evt instanceof TouchEvent) currentYRef.current = evt.touches[0].pageY;
			if (currentYRef.current < startYRef.current) return;
			const delta = currentYRef.current - startYRef.current;
			if (delta >= pullThresholdRef.current) setPullToRefreshThresholdBreached(true);
			if (delta > maxPullDownDistanceRef.current * 1.5) return;
			if (infScrollRef.current) {
				infScrollRef.current.style.overflow = "visible";
				infScrollRef.current.style.transform = `translate3d(0px, ${delta}px, 0px)`;
			}
		};
		const onEnd = () => {
			startYRef.current = 0;
			currentYRef.current = 0;
			draggingRef.current = false;
			setPullToRefreshThresholdBreached((breached) => {
				var _a;
				if (breached) (_a = refreshFunctionRef.current) === null || _a === void 0 || _a.call(refreshFunctionRef);
				return false;
			});
			requestAnimationFrame(() => {
				if (infScrollRef.current) {
					infScrollRef.current.style.overflow = "auto";
					infScrollRef.current.style.transform = "none";
					infScrollRef.current.style.willChange = "unset";
				}
			});
		};
		scrollEl.addEventListener("touchstart", onStart);
		scrollEl.addEventListener("touchmove", onMove);
		scrollEl.addEventListener("touchend", onEnd);
		scrollEl.addEventListener("mousedown", onStart);
		scrollEl.addEventListener("mousemove", onMove);
		scrollEl.addEventListener("mouseup", onEnd);
		return () => {
			scrollEl.removeEventListener("touchstart", onStart);
			scrollEl.removeEventListener("touchmove", onMove);
			scrollEl.removeEventListener("touchend", onEnd);
			scrollEl.removeEventListener("mousedown", onStart);
			scrollEl.removeEventListener("mousemove", onMove);
			scrollEl.removeEventListener("mouseup", onEnd);
		};
	}, [
		pullDownToRefresh,
		height,
		getScrollableNode
	]);
	const containerStyle = {
		height: height !== null && height !== void 0 ? height : "auto",
		overflow: "auto",
		WebkitOverflowScrolling: "touch",
		...style
	};
	const hasChildrenResolved = hasChildren || !!(children && children instanceof Array && children.length);
	const outerDivStyle = pullDownToRefresh && height ? { overflow: "auto" } : {};
	const sentinel = hasMore ? (0, import_jsx_runtime.jsx)("div", {
		ref: sentinelRef,
		style: { height: 1 }
	}) : null;
	return (0, import_jsx_runtime.jsx)("div", {
		style: outerDivStyle,
		className: "infinite-scroll-component__outerdiv",
		children: (0, import_jsx_runtime.jsxs)("div", {
			...ariaProps,
			role,
			tabIndex,
			id,
			className: ["infinite-scroll-component", className].filter(Boolean).join(" "),
			ref: infScrollRef,
			style: containerStyle,
			children: [
				pullDownToRefresh && (0, import_jsx_runtime.jsx)("div", {
					style: { position: "relative" },
					ref: pullDownRef,
					children: (0, import_jsx_runtime.jsx)("div", {
						style: {
							position: "absolute",
							left: 0,
							right: 0,
							top: -1 * maxPullDownDistance
						},
						children: pullToRefreshThresholdBreached ? releaseToRefreshContent : pullDownToRefreshContent
					})
				}),
				children,
				!showLoader && !hasChildrenResolved && hasMore && loader,
				showLoader && hasMore && loader,
				sentinel,
				!hasMore && endMessage
			]
		})
	});
}
//#endregion
export { InfiniteScroll as default, useInfiniteScroll };

//# sourceMappingURL=react-infinite-scroll-component.js.map