- Created `itemTemplateService.js` with all endpoints: `fetchItemTemplates`, `fetchProductGroups`, `fetchSubcategories`, `fetchSuppliers`, `fetchStoreProductsSearch`, `deleteItemTemplate`
- Added `/products` and `/product-groups` proxy entries to `vite.config.js`
- Wired real API data into `TemplateMainSection.jsx` — categories, subcategories, inventories, suppliers all fetched from endpoints
- Fixed `FilterDropdown` to handle both plain strings and `{ label, value }` objects
- Fixed stray `opt` variable error that was outside the component scope

- Fixed subcategory not loading by adding `onlyParents: false` to `fetchSubcategories`
- Fixed subcategory dropdown disabled logic to enable only when category has subcategories
- Wired `fetchItemTemplates` into `ItemTable` replacing dummy data
- Mapped API response fields to `ItemRow` expected shape
- Fixed `inStock` array error by normalizing with `Array.isArray`
- Added field mapping for `purchaseUnit`, `stockTakingUnit`, `basicMeasUnit` with prices
- Fixed `UnitCell` to show `-----` when unit is null/empty
- Implemented issue filter as frontend-only filter using `filteredItems`
- Implemented infinite scroll using `useInfiniteQuery` with intersection observer
- Added debounced search wired to API query
- Fixed category/subcategory/inventory/supplier dropdowns to store IDs alongside labels
- Fixed "No category/subcategory/inventory/supplier" options to clear respective filter instead of being non-clickable headers
- Added checkmark icon for selected dropdown item
- Lifted `search` and `filters` state to `ManageItemTemplatePage`
- Added empty state fallback for duplicate items filter with `GreenButton` to clear filter
- Increased table row height by removing fixed `height` and relying on padding
- Added empty state fallback for non-duplicate filters ("You have no products" with upload button)
- Fixed dropdown selected item styling (bg-grey, font-weight normal for selected, semibold for others)
- Lifted checkedIds state to ManageItemTemplatePage
- Moved selected bar (items selected badge + Assign supplier + Delete all buttons) from ItemTable into TemplateMainSection so it doesn't scroll away
- Fixed scroll issues with layout (h-full, flex-1 min-h-0, overflow-y-auto)
- Fixed sticky table header by adding `sticky` back to all `<th>` elements
- Removed stale `useEffect` referencing deleted `setSelectedBarHeight`
- Removed `selectedBarRef` declaration that was no longer needed
